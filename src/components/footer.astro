---
import { getCollection } from "astro:content";
const footerPages = await getCollection("legal");
---

<footer class="elevated">
    <nav aria-label="Footer">
        <ul>
            {footerPages.map(page => (
                <li>
                    <a href={`/legal/${page.slug}`}>{page.data.title}</a>
                </li>
            ))}
        </ul>
    </nav>
    <p class="copyright">© {new Date().getFullYear()} Pagels Kindergarten</p>
</footer>

<style>
    footer {
        margin-top: auto;
        border-top: 2px solid var(--color-border);
        border-bottom: none;
        border-radius: 0;
        text-align: center;
        padding: 1rem;
        display: flex;
        align-items: center;
        flex-direction: row;
        justify-content: space-around;
    }

    nav {
        display: flex;
        justify-content: center;
        align-items: center;
    }

    ul {
        list-style: none;
        display: flex;
        gap: 0.3rem 1.2rem;
        padding: 0.5rem;
        margin: 0;
        flex-wrap: wrap;
        justify-content: center;
    }

    ul li a {
        text-decoration: none;
        color: var(--color-text-light);
        font-size: clamp(0.9rem, 2vw, 1rem);
        font-weight: 600;
        position: relative;
        padding: 0.25rem 0;
        transition: color 0.3s ease;
    }

    ul li a:hover {
        color: var(--color-primary-medium);
    }

    ul li a::after {
        content: '';
        position: absolute;
        bottom: -2px;
        left: 50%;
        width: 0;
        height: 2px;
        background: var(--color-hover-accent);
        border-radius: 2px;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        transform: translateX(-50%);
        opacity: 0.7;
    }

    ul li a:hover::after {
        width: 100%;
    }

    .copyright {
        color: var(--color-text-light);
        font-size: 0.9rem;
        margin: 0;
        font-weight: 500;
    }
</style>